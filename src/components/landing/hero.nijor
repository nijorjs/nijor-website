<body>
    <div class="hero">
        <div class="hero-content">
            <h1>Nijor</h1>
            <p>Build modern web applications with reusable components, file-based routing, reactivity, and a clean developer experience.</p>
            <div class="actions">
                <a n:route="/docs" class="primary-btn">Get Started</a>
            </div>
        </div>
    </div>
</body>

<script>
    $.count = 0;
    export function add($){
        $.count++;
    }
</script>
<style>
    .hero {
        min-height: calc(100vh - 6rem);
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between;
        gap: 3rem;
        padding: 60px 40px;
    }

    .hero-content {
        flex: 1;
        max-width: 680px;
    }

    h1 {
        margin: 0;
        font-size: clamp(56px, 9vw, 92px);
        font-weight: 700;
        line-height: 1.15;
        letter-spacing: 1px;
        display: inline-block;
        background: linear-gradient(60deg, #00A2E8, #0e73f8, #00A2E8);
        background-size: 200% auto;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: flow 2s linear infinite;
    }

    @keyframes flow {
        0% {
            background-position: 0% center;
        }

        100% {
            background-position: 200% center;
        }
    }

    p {
        margin-top: 18px;
        font-size: 22px;
        line-height: 1.7;
        max-width: 620px;
        color: #2f3136;
    }

    .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        margin-top: 36px;
    }

    .primary-btn{
        display: inline-flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        font-weight: 600;
        font-size: 16px;
        padding: 14px 24px;
        border-radius: 999px;
        transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
    }

    .primary-btn {
        color: white;
        /* background: linear-gradient(120deg, #0e73f8, #00A2E8); */
        background-color: #0e73f8;
        box-shadow: rgba(14, 115, 248, 0.24) 0px 8px 24px;
    }

    .primary-btn:hover {
        background-color: #00A2E8;
        transform: translateY(-2px);
    }

    .hero-logo {
        flex-shrink: 0;
        width: min(100%, 500px);
        height: auto;
        object-fit: contain;
    }

    @media only screen and (max-width: 900px) {
        .hero {
            flex-direction: column;
            /* text-align: center; */
            padding: 50px 24px;
        }

        .hero-content {
            max-width: 100%;
        }

        .brand-logo {
            margin-left: auto;
            margin-right: auto;
        }

        p {
            margin-left: auto;
            margin-right: auto;
        }

        .actions {
            /* justify-content: center; */
        }

        .hero-logo {
            width: min(100%, 380px);
        }
    }
</style>

<style theme="dark">
    .hero {
        background-color: rgb(28, 28, 28);
    }

    p {
        color: #d3d7de;
    }
</style>